{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake Build", // 记住这个名字，下面要用
            "type": "shell",
            "command": "cmake --build build -j 5",
            "group": "build",
            "problemMatcher": ["$gcc"]
        }
    ]
}